all_cards: class : for all the cards
cartUpdater: event handler: function that modifies the cart when the number of item is entered or changed
cart: array : An array of objects that has 2 properties: (item)1 JSON object and another the (quantity)quantity of the object
arr_obj :
addToCartButtonGenerator():
totalCostCalculatonAndLocalStorage() - calculates and returns the total  
#item-list-in-cart: The div that is continuously getting re-rendered